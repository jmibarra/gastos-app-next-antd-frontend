"use client";
import { IExpense, IIncome } from "../models/expense.model";

export const copyPeriodData = async (originPeriod: string, destinationPeriod: string, authToken: string) => {
    alert(originPeriod);
    alert(destinationPeriod);
    alert(authToken);
};
